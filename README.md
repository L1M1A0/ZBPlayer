ZBPlayer，这是一款在 macOS 上使用的桌面app，主要功能是播放本地音频文件。
之所以会有这个项目，是因为在线播放的app有很多，但是对于一些拥有本地曲库的用户来说，相关支持却不太好，特别是一些拥有庞大曲库的用户。
对于一些用户，可能也会用到分表管理曲库的功能，这里有。
目前本播放器，已经实现了根据歌曲文件夹创建列表，选择多个文件夹，会创建多个列表，以分表的形式展示在列表区。
选择某一列表，我们会展示其所对应的文件路径下，所有的音频文件，并展示在右侧。
用户点击歌曲文件名，就可以播放了。

